services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: lh-order-tiktok-helper
    restart: unless-stopped
    ports:
      - "${PORT:-6600}:6600"
    env_file:
      - .env
    environment:
      NODE_ENV: production
      PORT: 6600
